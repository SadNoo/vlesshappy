# 10. 实施状态与验收边界

基线日期：2026-08-19；后端版本：`0.2.0-rc.1`。

## 已完成源码实现

| 能力 | 实现状态 | 本轮验证 |
|---|---|---|
| 固定 Xray 源码、最小 hook、离线 vendor 构建 | 已实现 | Docker amd64/arm64 编译通过 |
| 并发握手 1024 默认上限 | 已实现 | 未运行 |
| TCP 每用户 800、全局 3000 默认上限 | 已实现 | 未运行 |
| XUDP 每用户 128、全局 2048 默认上限 | 已实现 | 未运行 |
| Mux TCP/XUDP 子流统一计数 | 已实现 | 未运行 |
| 用户/节点 `node_speedlimit` 共享令牌桶 | 已实现 | 未运行 |
| 用户、凭据、策略定向撤销和增量用户同步 | 已实现 | 未运行 |
| `disconnect_ip` 目标来源撤销、连接器限制 | 已实现 | 未运行 |
| DNS 全候选 forbidden 检查和已检查 IP 固定 | 已实现 | 未运行 |
| 原子 outbox 与新增写/fsync/rename 故障注入缝 | 已实现 | 未运行 |
| MySQL COMMIT 响应丢失 fault proxy | 已实现 | 未运行 |
| 具体版本客户端矩阵和结果模板 | 已准备 | 未运行 |
| 72 小时/168 小时资源与探针采集脚本 | 已准备 | 未运行 |
| SBOM/漏洞/二进制/校验和/Sigstore 发布门禁脚本 | 已准备 | 未运行 |
| 独立 `?vless=1`、第一方混合订阅草案 | 已调整 | 未运行；未应用面板 |

## 本轮验证边界

项目负责人要求暂不运行测试。为交付容器，已完成 Linux amd64/arm64 的 Docker 编译并推送 `sadno/vle:1.0`；远端摘要和边界记录在 `docs/17-docker-image-1.0.md`。这只证明两个目标架构能够编译和上传，不等同于运行时、协议或发布门禁通过。

仍未运行 `go test`、race、vet、MySQL、PHP、真实客户端、故障注入、72 小时或 7 天任务，也没有生成正式 SBOM、漏洞报告或签名。`docs/11` 是 0.1.0 历史证据，不能继承到本版本。

## 仍需项目负责人提供或批准

1. 提供实际 SSPanel 文件后，重新生成最终集成；当前 `--apply` 会拒绝写入。
2. 提供第一方客户端文件和版本，补齐矩阵的阻塞行。
3. 决定本项目自身许可证；本轮没有擅自选择。
4. 提供临时测试节点、数据库和客户端环境后执行全部门禁。
5. 另行授权数据库迁移、生产部署、正式 tag、签名或 Release；后续远程 Git 和镜像发布仍按次授权。

因此源码实现已交付，但 `0.2.0-rc.1` 仍是“待执行验收”的候选版，不得在未跑门禁前标为生产正式版。
