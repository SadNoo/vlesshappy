# AGENTS.md

本文件适用于 `vlesshappy/` 及其全部子目录，是后续代理和自动化工作的目录级硬约束。

## 项目身份

- 项目名称：`vlesshappy`
- 对应远程仓库：`https://github.com/SadNoo/vlesshappy`
- 当前本地工作区：父项目内的 `vlesshappy/` 目录
- 当前目标：实现可生产使用的 VLESS + RAW/TCP + REALITY + Vision + XUDP 多用户后端，以及放在本目录内的 SSPanel 集成 overlay

远程仓库地址只用于识别项目。未经用户明确许可，不得 clone、fetch、push、创建分支、提交、开 PR、发 Release、修改远程设置或执行其他外部写操作。

## 允许范围

1. 只允许创建、读取、修改和删除 `vlesshappy/` 内的文件。
2. 不得修改父项目或相邻目录，包括但不限于 `app/`、`config/`、`resources/`、`sql/`、`test/`、`.git/` 和其他项目目录。
3. 面板集成必须先放在 `vlesshappy/panel-overlay/`，保持父项目相对路径；未经新许可不得应用到父项目。
4. 如果完成任务确实需要目录外修改，立即停止，列出目标文件、原因、最小改动和风险，等待用户授权。
5. 保留用户已有修改和未跟踪文件，不得覆盖、清理、移动或顺手格式化无关内容。

## 授权与范围控制

- 只实现用户明确批准的当前阶段和功能，不根据“以后可能需要”扩展范围。
- 不增加未获批准的协议、传输、管理后台、API、遥测平台、自动部署或云服务。
- 文档阶段不写实现；实现阶段不得擅自改变已确认文档中的协议、数据、计费和安全合同。
- 需求存在实质歧义且不同选择会改变兼容性、安全性或数据结构时，先停止并请求确认。
- 不得执行 destructive git、批量删除、数据库迁移应用、线上部署或远程状态变更，除非用户明确授权具体目标。

## 敏感信息

- 代码、文档、测试夹具、日志、提交和输出中不得包含真实密钥、密码、token、cookie、订阅地址、数据库凭据、私有 IP、用户邮箱或其他敏感信息。
- 示例统一使用明显无效的占位值，如 `example.com`、`127.0.0.1`、`<redacted>`。
- REALITY 私钥和数据库密码只允许从运行时 secret 文件读取，不得放入命令行、环境变量、普通配置文件、数据库、镜像或日志。
- 不读取、展示或复制与当前任务无关的系统、用户、浏览器、SSH、Docker 或云凭据。
- 测试必须使用生成的临时秘密；测试结束后不得把它们纳入版本控制。

## 实现原则

优先级固定如下：

1. 能正确使用。
2. 授权和秘密安全。
3. 计费与现有 V2 后端保持一致，允许数据库异常时出现少量漏记或重复。
4. 代码简短、直接、容易审阅。
5. 性能优化和扩展能力。

具体要求：

- 选择满足当前需求的最简单设计，避免框架化、插件化、元编程和无必要的抽象层。
- 优先使用小函数、清晰数据结构、显式控制流和标准库。
- 不为单一实现预先设计通用协议平台、复杂依赖注入、事件总线或多后端适配体系。
- 不重复实现 Xray-core 已提供的 VLESS、REALITY、Vision、XUDP、TLS 或密码学功能。
- 依赖保持最少并固定版本；引入依赖前说明标准库为何不够。
- 配置项只覆盖已确认的运行需求；拒绝未知字段，不堆积实验开关。
- 不把“兼容未来”作为增加复杂度的理由；需要时再按文档变更流程扩展。
- 注释解释不明显的安全、计费和并发原因，不复述代码表面行为。

## 数据与安全不变量

- 一个 VLESS 身份对应一个真实面板用户，不使用共享承运用户。
- 用户禁用、到期、超额、凭据或策略变化后，禁止新会话并撤销既有会话。
- 用户计费按节点倍率；节点带宽和流量日志保留原始字节语义。
- `sort=15` 的公开 REALITY 参数编码在现有 `ss_node.server`，不新增 VLESS 专用配置表。
- 流量按 V2 后端模式直接事务写入现有 `user`、`user_traffic_log` 和 `ss_node`；事务失败时恢复进程内计数，不新增 batch 表或本地 outbox。
- 数据库异常时允许少量漏记或重复计费，但授权过度陈旧或节点安全配置无效时仍 fail-closed。
- forbidden IP 必须检查域名解析后的最终 IP。
- VLESS UDP 明确为 XUDP，不宣传为原生 UDP 入站。
- REALITY 私钥永不进入数据库、订阅、日志和镜像。

## 修改和验证

- 开始前读取 `README.md` 及与任务相关的 `docs/` 文件。
- 修改尽量小且集中，不顺手重构无关代码。
- 使用与风险相称的最小验证：格式、单元测试、race、集成或端到端测试按阶段逐步增加。
- 修复 bug 时优先增加能复现问题的测试。
- 测试不得访问生产数据库、真实订阅或公网节点。
- 任何生成文件必须可追溯到本目录内脚本和固定依赖。
- 完成后确认所有变更路径都位于 `vlesshappy/`，并报告测试结果和未完成风险。

## Git 和交付

- 未经许可不执行 `git add`、`git commit`、`git push`、tag、PR 或 Release。
- 不修改父仓库历史、分支、工作树设置、submodule 或 remote。
- 交付时先列出改动文件、实现结果、验证结果和需要用户决定的事项。
- 不把“代码已写完”视为完成；只有相应阶段文档门禁通过，才能标记该阶段完成。

## 冲突处理

用户最新的明确指令优先。本文件与已确认设计文档冲突时，采用更严格的范围、安全和简化约束，并向用户指出冲突；不得自行放宽限制。
